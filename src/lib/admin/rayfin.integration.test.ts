import { describe, it } from "vitest";

describe.skip("Rayfin integration (requires backend)", () => {
	it("R1-R6 shared AdminData contract and smoke suite against RayfinAdminData", () => {
		// Enabled when Docker / a safe Rayfin deployment is available via npm run test:rayfin
	});
});
